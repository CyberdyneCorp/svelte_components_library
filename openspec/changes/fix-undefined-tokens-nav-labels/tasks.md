## 1. Tokens (#17)

- [x] 1.1 Rename synonym token references in core to existing foundation tokens
- [x] 1.2 Define new action (brand/secondary tint, danger), accent, syntax, glow, nav-height and video tokens in dark and light
- [x] 1.3 Alias `--btn-danger-*` to `--color-action-danger-*`
- [x] 1.4 Add `token-coverage.test.ts` and confirm it fails against the pre-fix sources

## 2. Navigation landmarks (#16)

- [x] 2.1 Add `ariaLabel` to `Sidebar` (default unlabelled) and `BottomNav` (default "Bottom navigation")
- [x] 2.2 Unit tests for default and custom labels; `Labelled` Sidebar story

## 3. Release

- [x] 3.1 Changeset: minor for foundation and core
